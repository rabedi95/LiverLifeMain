<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LiverLife.ForgotPasswordPage"
             Background="{StaticResource BackgroundGradient}"
             Title="">
    <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto" ColumnDefinitions="Auto,*" Padding="10">

            <Label Style="{StaticResource ForgotPasswordPageHeaderLabel}"/>
            <Label Style="{StaticResource ForgotPasswordPageBodyLabel}" />
            <Label Style="{StaticResource ForgotPasswordPageEmailLabel}" />
            <Entry Style="{StaticResource ForgotPasswordPageEmailEntry}"/>
            <Button Style="{StaticResource ForgotPasswordPageButton}"/>

    </Grid>
    
</ContentPage>